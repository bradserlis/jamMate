<br>
<br>

<%= console.log(currentUser.name, "we know its you on this page!")%>

<h1> <%=thisUser.name %> </h1>


	<ul>Instruments: </a>
<%thisUser.instruments.forEach(function(item){ %>
		<li><%= item %></li>
		<% }) %>
	</ul>
	<hr>
	<ul> Genres I Am Interested In Playing: </a>
		<%thisUser.genresToPlay.forEach(function(item){ %>
			<li><%= item %></li>
		<% }) %>
	</ul>
	
	<hr>
	
	<p>Location:<%= thisUser.zipcode %> </p>
	
	<br>
	<br> 
		<div class="col-sm-5 col-12">
            <button type="button" class="add btn btn-warning" data-toggle="modal" data-target="#commentModal">Leave a message</button>
 		</div>

<div class="modal fade messageText" id="commentModal" tabindex="-1" role="dialog" aria-labelledby="commentModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="commentlModalLabel">Leave a Message</h5>
      </div>
    <div class="modal-body">
        <form id="commentForm" data-id="<%= thisUser._id %>">
            <div class="form-group row">
                <label class="col-sm-4 col-form-label">Message:</label>
                <div class="col-sm-4">
                        <input type='text' class="form-control" name="message">	
                        </input>
                        <span class="help-block">You may want to leave contact info for this user to reach you at -- messaging in-app coming soon!</span>
                        <input type="text" class="form-control" name="author" value="<%=currentUser.name%>" hidden="true">
           		</div>
            <button type="submit" form="commentForm" class="commentSubmit"> Leave Message</button>
        	</div>
		</form>
	</div>
</div>
</div>
</div>
